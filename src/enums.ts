export enum FetchStatus {
  UNFETCHED = 'unfetched',
  FETCHING = 'fetching',
  SUCCESS = 'success',
  FAIL = 'fail',
}

export enum Status {
  ERROR = 'error',
  WARNING = 'warning',
  INFO = 'info',
  SUCCESS = 'success',
}
